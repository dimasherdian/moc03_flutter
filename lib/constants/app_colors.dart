import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand Colors
  static const Color primary = Color(0xFF66c0f4); // steamBlue
  static const Color secondary = Color(0xFF1b2838); // steamLightDark
  static const Color negative = Color(0xFFFF4D4D);

  // Background & Surface
  static const Color background = Color(0xFF171a21); // steamDark
  static const Color surface = Color(0xFF1b2838); 

  // Text & Border Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color border = Colors.black87;
  static const Color iconBackground = Color(0xFF2A475E);

  // State Colors
  static const Color disabledBackground = Color(0xFFBDBDBD);
  static const Color disabledText = Colors.white70;
}
